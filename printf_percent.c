#include "main.h"
/**
 * printf_percent - prints %
 *  @format: format
 * @per: va_list
 *
 * Return: number of characters printed
 */
int printf_percent(char *format, va_list per)
{
(void)format;
(void)per;
_putchar('%');
return (1);
}
